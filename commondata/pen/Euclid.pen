整数 a, b

「ユークリッドの互除法」を印刷する

「1つ目の値：」を改行無しで印刷する
a ← input()

「2つ目の値：」を改行無しで印刷する
b ← input()

「最大公約数は '」と euclid(a, b) と「' です」を印刷する


関数 整数 euclid(整数 x, 整数 y)
  | 整数 z
  | もし y != 0 ならば
  |   | z ← euclid(y, x % y)
  |   | z を返す
  | を実行し，そうでなければ
  |   | x を返す
  | を実行する
関数終了
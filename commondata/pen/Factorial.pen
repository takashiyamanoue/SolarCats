整数 n

「階乗を再帰的に求めるプログラムです」を印刷する
「整数を入力してください：」を改行なしで印刷する
n ← input()

n と「の階乗は '」と fact(n) と「' です」を印刷する

関数 整数 fact(整数 n)
  | 整数 x
  | もし n = 0 ならば
  |   | 1 を返す
  | を実行し，そうでなければ
  |   | x ← fact(n-1) * n
  |   | x を返す
  | を実行する
関数終了
整数 i
整数 ans
整数 inkey
ans ← random(99) + 1

iを1から5まで増やしながら，
  | inkey ← input()
  | もし ans > inkey ならば 「入力値より大きいです」を印刷する
  | もし ans < inkey ならば 「入力値より小さいです」を印刷する
  | もし ans = inkey ならば
  |   | 「正解です」を印刷する
  |   | i ← 10
  | を実行する
を繰り返す

もし i = 6 ならば 「正解は」と ans と「です」を印刷する

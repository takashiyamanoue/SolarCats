実数 sintyo
実数 taiju
実数 bmi

「身長を入力してください(cm)」 を印刷する
sintyo ← input()

「体重を入力してください(kg)」 を印刷する
taiju ← input()

bmi ← taiju / ( ( sintyo / 100 ) * ( sintyo / 100 ) )

もし bmi < 20 ならば
  | 「痩せています」を印刷する
を実行し，そうでなくもし bmi < 24 ならば
  | 「正常です」を印刷する
を実行し，そうでなくもし bmi < 26.5 ならば
  | 「やや肥満です」を印刷する
を実行し，そうでなければ
  | 「肥満です」を印刷する
を実行する
